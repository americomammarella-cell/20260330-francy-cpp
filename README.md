# 20260330-francy-cpp
test modulo cpp di Francesca  

per installare Google Benchmark lanciare i seguenti comandi:  
sudo apt update  
sudo apt install -y libbenchmark-dev  

per compilare: g++ -O2 benchmark.cpp -lbenchmark -lpthread -o bench  
per lanciare: ./bench
